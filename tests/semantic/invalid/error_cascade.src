fn main() -> int {
    bool a = 5;           // ошибка: int -> bool
    int b = a + 3;
    int c = b + 10;
    var d = "hello";
    int e = d + 1;        // ошибка: string + int не определено
    return b;
}