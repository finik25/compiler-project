fn inc() -> int {
    return 1;   // если вызовется – вернёт 1
}

fn main() -> int {
    int a = 0;
    // a != 0 ложно, поэтому inc() не должна вызываться
    if (a != 0 && inc() > 0) {
        return 1;  // если inc() вызвался – ошибка
    }
    return 0;      // правильный путь
}