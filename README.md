# MiniCompiler


**Учебный проект** по созданию компилятора для упрощённого C-подобного языка.  
Разрабатывается в рамках курса «Построение компиляторов» для изучения этапов трансляции: лексический, синтаксический и семантический анализ, генерация кода.

**Текущий этап (Sprint 1):** Лексический анализатор (сканер), преобразующий исходный код в последовательность токенов с полной диагностикой ошибок.


## Документация

- [Спецификация языка](docs/language_spec.md) — формальное описание лексем в EBNF.

---

## Быстрый старт (Quick Start)

1. **Установка в режиме разработки** (рекомендуется использовать виртуальное окружение):
   ```bash
   pip install -e .
   ```

2. **Запуск лексера на примере**:
   ```bash
   compiler lex --input examples/hello.src
   ```

   Пример файла `examples/hello.src`:
   ```
   fn main() {
       int x = 10;
       x++;
       // increment
       return x;
   }
   ```

   Ожидаемый вывод:
   ```
   1:1 KW_FN "fn"
   1:4 IDENTIFIER "main"
   1:8 LPAREN "("
   1:9 RPAREN ")"
   1:10 LBRACE "{"
   2:5 KW_INT "int"
   2:9 IDENTIFIER "x"
   2:11 ASSIGN "="
   2:13 INT_LITERAL "10" 10
   2:15 SEMICOLON ";"
   3:5 IDENTIFIER "x"
   3:6 INC_OP "++"
   3:8 SEMICOLON ";"
   4:5 KW_RETURN "return"
   4:12 IDENTIFIER "x"
   4:13 SEMICOLON ";"
   5:1 RBRACE "}"
   6:1 END_OF_FILE ""
   ```

3. **Запуск тестов**:
   ```bash
   pytest tests/ -v
   ```
   Все тесты (9 штук) должны проходить успешно.

---

## Структура проекта

```
compiler-project/
├── docs/                       # Документация
│   └── language_spec.md         # Спецификация языка
├── examples/                    # Примеры исходного кода
│   └── hello.src
├── src/                         # Исходный код компилятора
│   ├── __init__.py
│   ├── main.py                  # Точка входа CLI
│   ├── lexer/                   # Лексический анализатор
│   │   ├── __init__.py
│   │   ├── scanner.py            # Сканер
│   │   └── token.py              # Определение токенов
│   └── utils/                    # Вспомогательные модули (будущее)
│       └── __init__.py
├── tests/                        # Тесты
│   ├── __init__.py
│   ├── test_lexer.py             # Тесты лексера
│   └── lexer/
│       ├── valid/                 # Корректные входные файлы
│       │   ├── *.src
│       │   └── *.expected
│       └── invalid/               # Некорректные входные файлы
│           ├── *.src
│           └── *.expected
├── .gitignore
├── generate_expected.py          # Скрипт для перегенерации эталонов
├── README.md
└── setup.py                       # Установка пакета
```

---

## Системные требования

- Python 3.8 или выше
- pip
- (опционально) виртуальное окружение

---

## Разработка и внесение изменений

1. Клонируйте репозиторий.
2. Установите пакет в режиме разработки: `pip install -e .[dev]` (устанавливает также pytest).
3. Вносите изменения, пишите тесты.
4. Запускайте тесты перед коммитом: `pytest tests/ -v`.
5. При изменении формата вывода лексера перегенерируйте эталонные файлы:
   ```bash
   python generate_expected.py
   ```

---
